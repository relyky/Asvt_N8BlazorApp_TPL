# JSON 匯入/匯出功能使用說明

## 功能概述

決策樹編輯器現在支援完整的 JSON 匯入/匯出功能，讓您可以：
- 儲存決策樹為 JSON 檔案
- 從 JSON 檔案載入決策樹
- 在不同系統間分享決策樹結構
- 版本控制您的決策樹

## 匯出功能

### 1. 匯出 JSON 檔案
- 點擊工具列的「💾 匯出 JSON」按鈕
- 自動下載格式化的 JSON 檔案
- 檔名包含時間戳記，例如：`decision-tree-2024-12-24T10-30-45.json`

### 2. 匯出文字規則
- 點擊工具列的「📄 匯出規則」按鈕
- 選擇「文字規則 (IF/ELSE)」或「JSON 格式」
- 可以複製到剪貼簿或下載為檔案

### 3. 複製節點 JSON
- 選擇任何節點
- 在右側編輯面板中，切換到「JSON 格式」
- 點擊「📋 複製節點 JSON」按鈕

## 匯入功能

### 1. 從檔案匯入
- 點擊工具列的「📂 匯入 JSON」按鈕
- 選擇 JSON 檔案（副檔名 .json）
- 系統會驗證 JSON 格式
- 如果已有資料，會提示是否覆蓋

### 2. JSON 格式要求
必須包含以下欄位：
- `id`: 節點唯一識別碼
- `type`: 節點類型（IfCondition, ElseBranch, Assignment, None, Root）
- `children`: 子節點陣列
- `condition`: 條件（選填，僅 IfCondition 使用）
- `assignment`: 指定值（選填，僅 Assignment 使用）

## JSON 結構範例

### 基本結構
```json
{
  "id": "root",
  "type": "Root",
  "condition": "",
  "assignment": "",
  "children": [
    {
      "id": "node_001",
      "type": "IfCondition",
      "condition": "A = 3",
      "assignment": "",
      "children": [
        {
          "id": "node_002",
          "type": "Assignment",
          "condition": "",
          "assignment": "'3A'",
          "children": []
        }
      ]
    }
  ]
}
```

### 巢狀結構
```json
{
  "id": "root",
  "type": "Root",
  "children": [
    {
      "id": "node_001",
      "type": "IfCondition",
      "condition": "age >= 18",
      "children": [
        {
          "id": "node_002",
          "type": "IfCondition",
          "condition": "hasLicense = true",
          "children": [
            {
              "id": "node_003",
              "type": "Assignment",
              "assignment": "category = 'Driver'",
              "children": []
            }
          ]
        },
        {
          "id": "node_004",
          "type": "ElseBranch",
          "children": [
            {
              "id": "node_005",
              "type": "Assignment",
              "assignment": "category = 'Adult Non-Driver'",
              "children": []
            }
          ]
        }
      ]
    }
  ]
}
```

## 其他功能

### 清空樹
- 點擊「🗑️ 清空」按鈕
- 確認後會清空整個決策樹
- **注意：此操作無法復原！建議先匯出備份**

### 驗證結構
- 點擊「✓ 驗證結構」按鈕
- 系統會檢查：
  - 同層級不可有多個 ELSE
  - IF 節點必須有條件
  - Assignment 節點必須有指定值
  - **Assignment 節點不能有子節點（終止點規則）**
  - Assignment 節點後面建議不要有其他節點類型
  - 其他邏輯完整性

## 重要規則

### Assignment 終止點規則（兩個層面）
⚠️ **關鍵規則：Assignment 節點是決策樹的終止點（葉節點）**

#### 規則 1：Assignment 不能有子節點
- Assignment 節點**不能**有子節點
- Assignment 節點代表決策的最終結果
- 如果需要更多邏輯，應該在 Assignment 的**兄弟節點**中添加

**錯誤示例：**
```
IF age >= 18
  Assignment: result = 'Adult'    ← 終止點
    IF hasJob = true              ← ❌ 錯誤！不能在 Assignment 下
```

**正確示例：**
```
IF age >= 18
  IF hasJob = true
    Assignment: result = 'Working Adult'    ← 終止點
  ELSE
    Assignment: result = 'Adult'            ← 終止點
```

#### 規則 2：同層級唯一性 ⭐⭐ 新增
- **同一層級中，如果有 Assignment 節點，則不能有其他任何類型的節點**
- 包括：不能有其他 IF、ELSE，也不能有其他 Assignment
- 確保每個分支的結果唯一且明確

**錯誤示例 1：Assignment 與 IF 同層級**
```
IF age < 18
  Assignment: result = 'Minor'     ← ❌ 錯誤！
  IF needParent = true             ← ❌ 與 Assignment 同層級
    Assignment: guardian = required
```

**錯誤示例 2：Assignment 與 ELSE 同層級**
```
IF age = null
  Assignment: result = 'Unknown'   ← ❌ 錯誤！
  ELSE                             ← ❌ 與 Assignment 同層級
    Assignment: error = true
```

**錯誤示例 3：多個 Assignment 與其他類型混合**
```
IF condition
  Assignment: value1               ← ❌ 錯誤！
  Assignment: value2               ← ❌ 同層級有多個節點
  IF otherCondition                ← ❌ 與 Assignment 同層級
    Assignment: value3
```

**正確示例：**
```
IF age < 18
  IF needParent = true
    Assignment: guardian = required  ← ✓ 該層級只有這一個節點
  ELSE
    Assignment: guardian = optional  ← ✓ 該層級只有這一個節點
```

**為什麼需要這個規則？**
1. **邏輯清晰**：確保每個分支的結果是唯一且明確的
2. **避免混淆**：防止同時有結果賦值和條件判斷
3. **結構一致**：維持決策樹的標準結構（判斷→判斷→結果）

**如何修正違規結構：**
1. 將 Assignment 移到更深的層級
2. 將其他邏輯（IF/ELSE）提到 Assignment 之前
3. 重新設計決策樹結構，確保邏輯先判斷，最後才賦值

詳細說明請參閱：**ASSIGNMENT-SIBLING-RULE-GUIDE.md**

## 使用技巧

1. **定期匯出備份**
   - 在進行大幅修改前，先匯出 JSON
   - 可作為版本控制

2. **分享決策樹**
   - 匯出 JSON 檔案
   - 透過郵件或檔案分享系統傳送
   - 收件人可直接匯入使用

3. **範本管理**
   - 建立常用的決策樹結構
   - 匯出為範本 JSON
   - 需要時快速匯入

4. **除錯與檢查**
   - 使用節點 JSON 預覽功能
   - 複製節點 JSON 到外部編輯器
   - 檢查結構是否正確

## 錯誤處理

### 匯入失敗可能原因
1. JSON 格式不正確（語法錯誤）
2. 缺少必要欄位（id, type, children）
3. 節點類型不正確
4. 檔案編碼問題（請使用 UTF-8）

### 解決方式
1. 使用 JSON 驗證工具檢查格式
2. 參考範例 JSON 檔案
3. 確認所有必要欄位都存在
4. 確保檔案以 UTF-8 編碼儲存

## 範例檔案

提供了三個範例 JSON 檔案：

1. **sample-tree.json** - 簡單範例（✓ 正確結構）
   - 展示基本 IF/ELSE 結構
   - 適合初學者

2. **sample-tree-complex.json** - 複雜範例（✓ 正確結構）
   - 展示多層巢狀結構
   - 包含多個條件分支
   - 適合進階使用

3. **sample-tree-with-errors.json** - 錯誤範例（❌ 包含錯誤）
   - 展示常見的結構錯誤
   - 用於測試驗證功能
   - **包含 Assignment 節點有子節點的錯誤**
   - 匯入後請執行「驗證結構」查看錯誤報告

## 技術細節

- JSON 使用 2 空格縮排
- 自動產生唯一的節點 ID
- 支援無限層級的巢狀結構
- 檔案大小無限制（受瀏覽器記憶體限制）
